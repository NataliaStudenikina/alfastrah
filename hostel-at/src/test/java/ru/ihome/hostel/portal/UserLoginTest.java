package ru.ihome.hostel.portal;
import org.junit.Test;
import ru.ihome.hostel.HostelTestSupport;
import java.io.IOException;

public class UserLoginTest extends HostelTestSupport {

    @Test
    public void userLoginTest() throws InterruptedException, IOException {
        //loginAsUser(USER_LOGIN, USER_PASSWORD);
        Thread.sleep(1000);
        driver.close();
    }

}
